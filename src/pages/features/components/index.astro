---
import Layout from "../../../layouts/Layout.astro";
import SubNav from "../../../components/SubNav.astro";
import FadeInOnScroll from "../../../components/FadeInOnScroll.astro";
---

<Layout title="Components - Sofondo Mostra">
  <SubNav variant="simple">
    <a href="/features/">Overview</a>
    <a href="/features/scrolling/">Smooth Scrolling</a>
    <a href="/features/animations/">Animations</a>
    <a href="/features/components/" class="active">Components</a>
  </SubNav>

  <section class="page-content" data-bg-theme="light">
    <div class="container">
      <FadeInOnScroll>
        <div class="page-header">
          <h1 class="page-title">Components</h1>
          <p class="page-description intro">
            Pre-built components included in the template.
          </p>
        </div>
      </FadeInOnScroll>

      <div class="content">
        <FadeInOnScroll delay={0}>
          <section class="content-section">
            <h2>Header</h2>
            <p>
              A responsive header with logo, navigation links, and a CTA button.
              Includes a mobile menu with smooth animations.
            </p>
            <div class="code-block">
              <pre><code>&lt;Header /&gt;</code></pre>
            </div>
          </section>
        </FadeInOnScroll>

        <FadeInOnScroll delay={100}>
          <section class="content-section">
            <h2>SubNav</h2>
            <p>
              A sticky sub-navigation with glassmorphism effects.
              Supports two variants: simple and services.
            </p>
            <div class="code-block">
              <pre><code>&lt;!-- Simple variant --&gt;
&lt;SubNav variant="simple"&gt;
  &lt;a href="/docs/" class="active"&gt;Getting Started&lt;/a&gt;
  &lt;a href="/docs/config/"&gt;Configuration&lt;/a&gt;
&lt;/SubNav&gt;

&lt;!-- Services variant with grouped links --&gt;
&lt;SubNav variant="services"&gt;
  &lt;div class="sub-nav-group sub-nav-group-design"&gt;
    &lt;a href="/services/web/"&gt;Web Design&lt;/a&gt;
  &lt;/div&gt;
&lt;/SubNav&gt;</code></pre>
            </div>
          </section>
        </FadeInOnScroll>

        <FadeInOnScroll delay={200}>
          <section class="content-section">
            <h2>FadeInOnScroll</h2>
            <p>
              Wrapper component for scroll-triggered animations.
              Supports configurable delays for staggered effects.
            </p>
            <div class="code-block">
              <pre><code>&lt;FadeInOnScroll delay={'{'}100{'}'}&gt;
  &lt;div&gt;Content that fades in&lt;/div&gt;
&lt;/FadeInOnScroll&gt;</code></pre>
            </div>
          </section>
        </FadeInOnScroll>

        <FadeInOnScroll delay={300}>
          <section class="content-section">
            <h2>Logo</h2>
            <p>
              Flexible logo component that supports an optional logo mark
              with the site name and template name.
            </p>
            <div class="code-block">
              <pre><code>&lt;Logo width={'{'}140{'}'} height={'{'}32{'}'} /&gt;</code></pre>
            </div>
          </section>
        </FadeInOnScroll>

        <FadeInOnScroll delay={400}>
          <section class="content-section">
            <h2>Footer</h2>
            <p>
              A simple footer with copyright notice and configurable links.
            </p>
            <div class="code-block">
              <pre><code>&lt;Footer /&gt;</code></pre>
            </div>
          </section>
        </FadeInOnScroll>

        <FadeInOnScroll delay={500}>
          <section class="content-section">
            <h2>Navigation</h2>
            <p>
              React-based navigation with desktop menu and mobile drawer.
              Includes smooth scrolling for the mobile menu.
            </p>
            <div class="code-block">
              <pre><code>&lt;Navigation client:load /&gt;</code></pre>
            </div>
          </section>
        </FadeInOnScroll>
      </div>
    </div>
  </section>
</Layout>

<style>
  .page-content {
    padding: 2rem 0 4rem;
  }

  .page-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .page-title {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 600;
    margin-bottom: 1rem;
    color: var(--text-primary);
  }

  .page-description {
    font-size: 1.25rem;
    color: var(--text-secondary);
    max-width: 600px;
    margin: 0 auto;
  }

  .content {
    max-width: 800px;
    margin: 0 auto;
  }

  .content-section {
    margin-bottom: 3rem;
  }

  .content-section h2 {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 1rem;
    color: var(--text-primary);
  }

  .content-section p {
    font-size: 1rem;
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 1rem;
  }

  .code-block {
    background: #1e1e2e;
    border-radius: 12px;
    padding: 1.5rem;
    overflow-x: auto;
    margin: 1rem 0;
  }

  .code-block pre {
    margin: 0;
  }

  .code-block code {
    font-family: 'Fira Code', 'Monaco', 'Consolas', monospace;
    font-size: 0.875rem;
    color: #cdd6f4;
    line-height: 1.6;
  }
</style>
